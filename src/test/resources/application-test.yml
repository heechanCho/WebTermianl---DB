spring:
  datasource:
    url: jdbc:mysql://localhost:3306/webTerminal_test
    username: user
    password: userooopklj
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
